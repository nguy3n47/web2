<% if (conferences) { %> <% conferences.forEach(function(conference, index){ %>
<!-- <div class="list-group"></div>
      <a href="#" class="list-group-item list-group-item-action">
        <div>
          <h5><%= conference.name %></h5>
          <small class="text-muted"><%= conference.time %></small>
        <p><%= conference.description %></p>
        <div id="html-list-<%= index + 1 %>"/>
          <script type="text/javascript">
            document.querySelector("#html-list-<%- index + 1 %>").innerHTML = `<%- conference.fullDescription %>`;
            </script>
          </div>
      </a>
      </div> -->

<div class="card mt-5">
  <h5 class="card-header"><%= conference.name %></h5>
  <div class="card-body">
    <p class="card-title"><%= conference.description %></p>
    <p class="card-text" id="html-list-<%= index + 1 %>">
      <script type="text/javascript">
        document.querySelector(
          "#html-list-<%- index + 1 %>"
        ).innerHTML = `<%- conference.fullDescription %>`;
      </script>
    </p>
  </div>
  <div class="card-footer text-muted"><%= conference.time %></div>
</div>
<% }); %> <% } %>
